export interface UserSettings {
  autoPlay: boolean;
  autoHighlight: boolean;
  autoScroll: boolean;
}
export const defaultUserSettings: UserSettings = {
  autoPlay: true,
  autoHighlight: true,
  autoScroll: true,
};
